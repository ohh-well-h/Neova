# Leave both values empty to run Neova in local demo mode.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Optional hosted web checkout or server endpoint that starts checkout.
# Use HTTPS in production. Leave empty until billing is configured.
EXPO_PUBLIC_WEB_CHECKOUT_URL=
